# CalculeMedia
// caulcular a media

function calcularMedia(numero1, numero2){
const soma = numero1+numero2
    const media = soma/2
    return media;
}
let n1 = Number(prompt("digite um primeiro valor"))
const n2 = Number(prompt("digite um segundo valor"))

const resultado=calcularMedia(n1,n2);
console.log(resultado);
----------------------------------------------------------------------------------

//leia o codigo abaixo

function minhaFuncao(variavel) {
	return variavel * 5
}

minhaFuncao(2)
minhaFuncao(10)


//a. Explique o que essa função faz e qual é sua utilidade?
// o console vai imprimir 10 e 50, vai multiplicar ambos os valores por 5;

//b .O que aconteceria se retirasse os dois console.log e simplesmente invocasse a função minhaFuncao(2) e minhaFuncao(10)
//Live reload enabled.

